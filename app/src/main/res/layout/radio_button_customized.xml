<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <de.fruity.coffeeapp.ui_elements.RadioButtonCustomized
        android:background="#ccc"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:paddingBottom="40dp"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        app:idleDrawable="@android:drawable/ic_menu_add"
        app:selectedFirstDrawable="@android:drawable/ic_menu_add"
        app:selectedSecondDrawable="@android:drawable/ic_menu_add" />

</FrameLayout>
